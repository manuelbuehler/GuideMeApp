<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GuideMeApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GuideMeApp"
    xmlns:views="clr-namespace:GuideMeApp.Views"
    xmlns:icons="clr-namespace:GuideMeApp"
    Shell.FlyoutBehavior="Disabled"
    Title="GuideMeApp">

    <TabBar>
        <ShellContent
            Title="Buchen"
            ContentTemplate="{DataTemplate local:MainPage}"
            Icon="dashboard.svg"/>


        <ShellContent 
            Title="Meine Ausflüge"
            ContentTemplate="{DataTemplate views:MyTripsPage}"
            Icon="add.svg"/>

        <ShellContent 
            Title="Karte"
            ContentTemplate="{DataTemplate views:MapPage}"
            Icon="map.svg"/>

        <ShellContent 
            Title="Einstellungen"
            ContentTemplate="{DataTemplate views:ProfilePage}"
            Icon="setting.svg"/>
    </TabBar>
</Shell>
